import SwiftUI

enum BrandPalette {
    static let navy = Color(red: 0.00, green: 0.10, blue: 0.32)
    static let deepNavy = Color(red: 0.00, green: 0.06, blue: 0.22)
    static let card = Color(red: 0.03, green: 0.14, blue: 0.40)
    static let cardSoft = Color(red: 0.06, green: 0.18, blue: 0.46)
    static let yellow = Color(red: 1.00, green: 0.79, blue: 0.05)
    static let white = Color.white
    static let secondaryText = Color.white.opacity(0.72)
    static let mutedText = Color.white.opacity(0.54)
    static let sky = Color(red: 0.20, green: 0.54, blue: 0.88)
    static let clay = Color(red: 0.72, green: 0.24, blue: 0.18)
}
